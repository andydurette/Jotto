/**
 * @function guessWordsReducer
 * @param {array} state - Array og guessed words.
 * @param {object} action - action to be reduced.
 * @returns {array} - new guessedWords state.
 */

export default (state, action) => {
  return null;
}